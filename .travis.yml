language: php

git:
  depth: 1

jobs:
  include:
    - &test
    - stage: Test
      php: "7.3"
      os: linux
      install:
        - travis_retry composer self-update
        - travis_retry composer install --no-interaction --prefer-source --dev
      script:
        - composer test
        - composer lint
    - <<: *test
      stage: Compatibility
      php: "5.6"
    - <<: *test
      php: "7.2"
    - <<: *test
      php: "7.1"
    - <<: *test
      stage: Coverage
      name: Coveralls
      script:
        - composer coveralls
